// 未完成

auto.waitFor();

const w = device.width;
const h = device.height;
toast(w, h);
swipe(w/2, h-100, w/2, h/2, 1000);
// 本想手势关，但滑动断断续续的

// app.openAppSetting('com.wisedu.cpdaily');
// vscode上调试不行，手机上直接运行可以
